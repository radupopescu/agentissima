# Operations

## Daily checks

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Incident reviews are held within five working days and produce a short written summary.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

## Backups

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Incident reviews are held within five working days and produce a short written summary.

Incident reviews are held within five working days and produce a short written summary.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

## Migrations

Incident reviews are held within five working days and produce a short written summary.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Incident reviews are held within five working days and produce a short written summary.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

## Alerting

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Incident reviews are held within five working days and produce a short written summary.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

## Access

Incident reviews are held within five working days and produce a short written summary.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Incident reviews are held within five working days and produce a short written summary.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Incident reviews are held within five working days and produce a short written summary.

## Runbook references

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Incident reviews are held within five working days and produce a short written summary.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

Access to the production store is granted for a fixed period and revoked automatically on expiry.

The runbook reference for a failed export is OPS-4417; quote it when raising a ticket.

## Incidents

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Incident reviews are held within five working days and produce a short written summary.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Incident reviews are held within five working days and produce a short written summary.

Incident reviews are held within five working days and produce a short written summary.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

## Escalation

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Where a check fails, the on-call engineer raises a ticket before attempting any remediation.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Alert thresholds are reviewed quarterly; changes require sign-off from the service owner.

Schema migrations are applied in a maintenance window and are always reversible within the same release.

Routine checks are performed at the start of each working day and the results recorded in the shared operations log.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.

Incident reviews are held within five working days and produce a short written summary.

Incident reviews are held within five working days and produce a short written summary.

Backups of the file store are verified weekly by restoring into a scratch directory and comparing checksums.
